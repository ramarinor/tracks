import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const SingupScreen = () => {
  return <Text style={{ fontSize: 48 }}>SingupScreen</Text>;
};

const styles = StyleSheet.create({});

export default SingupScreen;
